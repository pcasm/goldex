import { CustomersDatasource } from './customers.datasource';

describe('Customers.Datasource', () => {
  it('should create an instance', () => {
    expect(new CustomersDatasource()).toBeTruthy();
  });
});
