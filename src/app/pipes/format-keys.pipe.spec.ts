import { FormatKeysPipe } from './format-keys.pipe';

describe('FormatKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
